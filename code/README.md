# code 目录说明

当前结构按功能拆分，避免混用：

- `collection/`
  - 仅用于 MI 数据采集（连接设备、提示受试者、保存数据）
- `training/`
  - 仅用于读取采集数据并训练模型
- `realtime/`
  - 仅用于加载已训练模型做实时判别
- `viewer/`
  - 仅用于查看 `epochs.npz`（数字统计 + 波形可视化）
- `shared/`
  - 各模块共享的底层逻辑
- `legacy/`
  - 历史代码，不参与当前主流程
